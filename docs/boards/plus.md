# Onewheel+

**Status: essentially unknown.** We have no board-specific data for this
model at all -- this page exists as a placeholder so a contributor knows
where their findings go, not because we've established anything.

## Identity

- Firmware generation: likely `2` or `3` in the release-order pattern (see
  the [Pint page](pint.md#identity)) -- this is our least confident guess in
  the whole generation table, since Onewheel+'s exact place relative to the
  original V1 hasn't been checked against anything.
- Tire diameter: unknown.

## Riding modes

Unknown -- no mode-name resources identified for this model.

## Unlock scheme

Unknown. Likely old enough firmware that the local MD5 scheme (or something
close to it) applies rather than server-gating, but this is a guess based on
age, not evidence.

See [CONTRIBUTING.md](../../CONTRIBUTING.md) for how to establish any of
this if you have one of these boards.
